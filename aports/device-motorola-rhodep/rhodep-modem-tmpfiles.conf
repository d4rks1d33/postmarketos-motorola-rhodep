# The modem writes its persistent configuration here (MCFG, datablocks, ticf).
# Without this its init stalls and the watchdog (DOG) kills it after ~65s.
d /readwrite 0755 root root -
d /readwrite/datablock 0755 root root -
