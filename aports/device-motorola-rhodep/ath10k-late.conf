# Do not let udev autoload ath10k_snoc: at boot the WLAN protection domain
# (wlan_pd, inside the modem) does not exist yet and the QMI client fails,
# then waits silently forever. ath10k-late.service loads it once pd-mapper
# has registered QMI service 69 (wlfw).
blacklist ath10k_snoc
